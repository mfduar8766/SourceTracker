import React from 'react';
import openOffice from './assets/openOffice.jpg';

const App = () => (
  <main style={{ flexGrow: 1 }}>
    <img src={openOffice} style={{ width: '100%', height: '100%' }} />
  </main>
);

export default App;
